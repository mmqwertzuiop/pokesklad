name: Scrape Pokemon TCG Shops

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

concurrency:
  group: scraper
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 65
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install cheerio @supabase/supabase-js puppeteer
      - name: Run scrapers in loop (~2 min fast, puppeteer every 3rd)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: false
        run: |
          INTERVAL=120
          END=$((SECONDS + 3300))
          RUN=1
          while [ "$SECONDS" -lt "$END" ]; do
            START=$SECONDS
            echo "=== Run $RUN at $(date -u) ==="
            node scripts/scrape.js --run=$RUN || echo "Scrape failed, continuing..."
            ELAPSED=$((SECONDS - START))
            echo "Run $RUN took ${ELAPSED}s"
            RUN=$((RUN + 1))
            REMAINING=$((INTERVAL - ELAPSED))
            if [ "$REMAINING" -gt 0 ] && [ "$SECONDS" -lt "$END" ]; then
              sleep "$REMAINING"
            fi
          done
          echo "=== Loop finished: $((RUN - 1)) runs ==="
